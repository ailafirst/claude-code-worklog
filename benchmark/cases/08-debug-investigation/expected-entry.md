# 金标准条目 — 08-debug-investigation

```markdown
## 18:20 · session

---
date: 2026-06-30
project: payments
threads: ["prod-incident-0630", "conn-pool"]
head: <由 append 回填>
blockers: ["webhook 延迟 14:00 起 ~10x（P99 80→800ms）未根因；强相关于该次部署把并发 8→16，疑似连接池 max=10 争用，仅时间相关无直接证据"]
next: ["staging 把 pool max 提到 20 回放生产流量看是否回落", "或线上并发回滚到 8 确认延迟掉回 ~80ms（任一即可定性）"]
---

**做成了什么**
- 纯调查（无 commit）：把 webhook 延迟尖峰缩到"并发 8→16 + 连接池 max=10 争用"这个假设

**关键决策 / 为什么**
- 排除法定位：DB query 耗时平稳 → 非数据库；支付商 status 绿、调用耗时不变 → 非上游；
  延迟拐点与 14:00 部署（并发 8→16）时间完全重合 → 锁定并发/连接池方向。

**卡点 / 困惑**
- 假设未证实：只有时间相关性，无直接证据。工作区加了一行计时日志，样本还不够。

**下次 TODO**
- 见 next：提 pool / 回滚并发，二选一即可证实

**碰到的 open thread**
- conn-pool：池 max=10 与并发上限的关系可能不止影响 webhook，值得复盘容量配置
```

## 评分要点

**必须命中（本 case 命门）**
- [ ] **不因 0 commit 写成空条目**——这是高价值 session。
- [ ] 记下**排除了什么**（DB、外部商）和**强相关线索**（14:00 部署 / 并发 8→16）。
- [ ] 假设标注为**未证实**，并给出可证实的下一步（提 pool / 回滚并发）。

**anti-pattern**
- 输出"今天无代码改动"或仅一句"在排查线上延迟问题"。
- 把未证实的连接池假设写成已确认的根因。
